#pragma once
#include "Utils/HostDeviceShared.slangh"

struct UniformGrid
{
    float3 pos = float3(0, 0, 0);
    float3 intensity = float3(0, 0, 0);
    uint2 range = uint2(0, 0);

    uint rootNode = 0; // TODO: rename
    bool isLeafNode = false;
};